# ============================================================
# Frontend Production Environment Variables
# This file must exist BEFORE running `vite build`
# so that VITE_* values are baked into the bundle correctly.
# ============================================================

VITE_API_ROOT=https://your-domain.com
